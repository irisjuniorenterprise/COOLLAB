{% extends 'base.html.twig' %}

{% block title %}ParticipateToCompetition index{% endblock %}

{% block body %}
    <h1>ParticipateToCompetition index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>IsPresent</th>
                <th>ParticipatedAt</th>
                <th>Classification</th>
                <th>Prize</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participate_to_competition in participate_to_competitions %}
            <tr>
                <td>{{ participate_to_competition.id }}</td>
                <td>{{ participate_to_competition.isPresent ? 'Yes' : 'No' }}</td>
                <td>{{ participate_to_competition.participatedAt ? participate_to_competition.participatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ participate_to_competition.classification }}</td>
                <td>{{ participate_to_competition.prize }}</td>
                <td>
                    <a href="{{ path('app_participate_to_competition_show', {'id': participate_to_competition.id}) }}">show</a>
                    <a href="{{ path('app_participate_to_competition_edit', {'id': participate_to_competition.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_participate_to_competition_new') }}">Create new</a>
{% endblock %}
