<form method="post" action="{{ path('app_participate_to_competition_delete', {'id': participate_to_competition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participate_to_competition.id) }}">
    <button class="btn">Delete</button>
</form>
