<form method="post" action="{{ path('app_training_trainer_delete', {'id': training_trainer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_trainer.id) }}">
    <button class="btn btn-outline-primary"><i class="fa fa-trash" aria-hidden="true"></i></button>
</form>
